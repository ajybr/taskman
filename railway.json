{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "bun",
    "buildCommand": "bun run --filter=web build && bun run --filter=server build"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}